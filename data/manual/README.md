# data/manual/

Only `logos-original/` lives here now — the untouched source SVGs, backed up once
by `npm run logos:recolor`.

**All hand-curated data moved to [`data/staging/`](../staging/README.md)** — the
`_staging_*` (numbers) and `_blurb_*` (words) layer that `build-data.mjs` compiles
into `public/data/`.
