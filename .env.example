# CollegeFootballData.com API key — free at https://collegefootballdata.com/key
# Used only by `npm run build:data` to refresh the auto-updatable stats
# (AP poll weeks, all-time wins/record, NFL draft picks, official team colors).
# Without it, build:data falls back to the values in data/manual/*.csv.
CFBD_API_KEY=
