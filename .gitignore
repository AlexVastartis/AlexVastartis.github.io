node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
.vscode/*
!.vscode/extensions.json
scripts/tmp/
# data/staging/ (the hand-maintained data store) and data/archive/ (frozen raw
# source dumps) are BOTH committed on purpose — the build reads staging, never the network
